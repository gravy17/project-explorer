/**
 * @jest-environment jsdom
 */

//Imports and variables 

// Setup
beforeAll(() => {
    
});

afterAll(() => {
});

// Tests to ensure views reflect last visit to the db
describe('View Tracking Functionality in the Views', () => {

    beforeEach(() => {

    })
    
    afterEach(() => {
    
    });

    it('A logged in user should see a date on a viewed project', () => {
    //Setup: Login, identify first project on homepage without date
    //Act: Open project and then return to homepage
    //Verify: Expect a date in the same project(use _id)
    });
});