/**
 * @jest-environment node
 */

// Imports and Variables

// Setup
beforeAll(() => {
    
});

afterAll(() => {
});


// Tests to ensure server stores visits to a project to the db
describe('View Tracking Functionality in the Models, Services and Controllers', () => {
    
    beforeEach(() => {

    })
    
    afterEach(() => {
    
    });

    it('Record of view is stored on visit to a page', () => {
    // Setup: Login
    // Act: Visit project route for a specific project
    // Verify: userService track view was called on visit to /project/:id
    // Or same date and project id can be found in the db under user 
    });
});